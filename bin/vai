#!/bin/bash

for instance in abba order four "aman-aplan" triples ranges words
do
    for depth in {5..15}
    do
        echo run localsearch -- --name $instance --depth $depth # >> ~/Desktop/resultados.csv
        npm run localsearch -- --name $instance --depth $depth >> ~/Desktop/resultados_ILS.csv
    done
done

